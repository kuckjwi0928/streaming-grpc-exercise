apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "streaming-grpc.fullname" . }}
  labels:
    {{- include "streaming-grpc.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "streaming-grpc.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "streaming-grpc.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image }}"
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
